# scAnt API server — common dependencies
fastapi
uvicorn
requests
numpy
pydantic
opencv-python-headless
pytest
httpx

# picamera2 — RPi-specific, install only on Raspberry Pi (ARM)
# On PC/CI, skip this line or use: pip install -r requirements.txt --no-deps
# picamera2